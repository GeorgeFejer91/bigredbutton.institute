# AI Agent First Read

Before changing this project, read:

```text
For-AI/START_HERE.md
```

This is a native Meta Quest mixed-reality study app, not Unity and not a browser app. Preserve the participant-facing experiment flow, local data export contract, and validation ladder unless the user explicitly changes the study design.

The final participant interaction is controller-based physical pressing of the 3D Big Red Button. Do not replace it with gaze, Android taps, keyboard input, visible flat 2D buttons, or validation automation.

Do not commit APKs, build output, logs, screenshots, headset exports, or local machine paths. Keep those under ignored `artifacts/`, `local-export-pulls/`, or build folders.
