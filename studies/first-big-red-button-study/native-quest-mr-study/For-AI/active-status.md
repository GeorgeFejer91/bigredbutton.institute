# Active Status

## Current Snapshot - 2026-06-13 Centralized Runtime Audio Library

- Latest debug APK after centralizing runtime audio: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `A9325805CC18021A7BFA3B74E2B506EF43B910334581720B31CF0F717B3B098D`. It was installed on Quest 3S `3487C10J0P01ZY`.
- Runtime study audio now lives under `../audio-assets/localized`: English speech in `en_us`, Japanese speech in `ja_jp`, and shared non-speech cues in `shared/questionnaire_ui`, `shared/questionnaire_transition`, `shared/button_press`, and `shared/inactive`. Gradle stages this library with a `Sync` task into `assets/localized/**`, so stale generated files are removed before packaging.
- The app no longer uses `R.raw` or `app/src/main/assets/sfx` for participant-facing runtime audio. Static validation now fails if app raw/SFX audio files or `R.raw.*` playback references return.
- APK contents were checked after rebuild: study audio is packaged only under `assets/localized/**`, with no loose `assets/localized/shared/*.wav|mp3|ogg` entries and no old root instruction MP3/SFX entries.
- Local gates passed: `tools/update-localized-audio-manifest.ps1`; `tools/validate-localized-audio.ps1 -RequireJapaneseAudio` at `artifacts/localized-audio-validation/localized-audio-validation-20260613-203901.json`; exact audio validation at `artifacts/audio-validation/audio-validation-20260613-203921.json`; synthetic export schema validation at `artifacts/export-schema-validation/export-schema-validation-20260613-203922.json`; static study validation at `artifacts/local-validation/validation-20260613-204209.json`; and `tools/build-apk.ps1`.
- Headset gates passed on the current APK: English/default qkv `artifacts/qkv/20260613-204238/quest-keyevent-questionnaire-validation-summary.json` and Japanese override qkv `artifacts/qkv/20260613-204352/quest-keyevent-questionnaire-validation-summary.json`. Both passed export schema validation and export mirror comparison. Logcat shows `fallbackCount=0`; English opens `localized/en_us/...`, Japanese opens `localized/ja_jp/...`, and shared cues open `localized/shared/...`.

## Current Snapshot - 2026-06-13 Radial Pop-Out Name Keyboard Verified On APK

- Latest debug APK after changing Name entry to a headset-centered radial pop-out keyboard: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `4646E23DBF3C7F893F4068F9F5829C2CDCB2D7ADAF230A84080093DEA0423C6B`, size `164708065` bytes. This APK was installed on Quest 3S `3487C10J0P01ZY`.
- Name uses a separate visible app-owned spatial `keyboard_panel`, not the Quest/system IME and not an integrated section inside the questionnaire panel. Placement is now defined from the participant/headset field of view: the questionnaire sits on the central radial ray, the keyboard sits on a neighboring left radial ray at `angleDeg=-36`, `distanceM=1.32`, `widthM=0.50`, and both panels use the SDK panel-facing orientation that renders toward the headset. Runtime evidence must include `radialReference=headset_center`, `orientation=faces_headset`, `placement=left_of_questionnaire_near_user`, `nonObstructing=true`, and `fovVisible=true`.
- The headset visual proof is `artifacts/qdk/20260613-165147/demographics-directional-keyboard-name-entry.png`: the keyboard is close to the participant in the left visual angle, fully visible in the headset capture, and the main questionnaire remains readable. `artifacts/qdk/20260613-165147/quest-demographics-directional-keyboard-export-validation-summary.json` passed using only D-pad Up/Down/Left/Right plus Enter to navigate the pop-out keyboard, set Name to `George Fejer`, set Age to `34`, pull JSON/CSV exports, and byte-compare the `BigRedButtonFirstStudyExports` and `ExperimentResults` mirrors.
- Current local gates passed on this source/APK state: build via `tools/build-apk.ps1`, keyboard contract `artifacts/native-keyboard-validation/native-keyboard-validation-20260613-170037.json`, static study validation `artifacts/local-validation/validation-20260613-170038.json`, and updated radial keyboard layout preview `artifacts/layout-previews/preview-20260613-165900/demographics-app-owned-keyboard-preview.png`.
- Current Quest 3S `3487C10J0P01ZY` headset gates passed on this APK: direct raw keyevent validation `artifacts/quest-demographics-direct-keyboard/20260613-165417/quest-demographics-direct-keyboard-validation-summary.json`; focused app-owned keyboard smoke `artifacts/quest-demographics-keyboard/20260613-165508/quest-demographics-keyboard-validation-summary.json`; repeated-entry stress `artifacts/quest-demographics-keypress-stress/20260613-165555/quest-demographics-keypress-stress-summary.json`; D-pad-only keyboard/export validation `artifacts/qdk/20260613-165147/quest-demographics-directional-keyboard-export-validation-summary.json`; full qkv questionnaire/export regression `artifacts/qkv/20260613-165645/quest-keyevent-questionnaire-validation-summary.json`; and panel/glitch smoke `artifacts/quest-panel-smoke/20260613-165755/quest-panel-smoke-summary.json`.
- The direct raw keyevent gate focused Name, sent `Georgex`, Backspace, Space, `Fejer`, Enter, then set Age to `34` with D-pad events. It verified the multi-character Name path, Backspace, Enter-to-Age, Age D-pad slider path, and no system IME dependency. The stress gate retained repeated full-name updates and confirmed Age `34`. The qkv gate passed the whole fast questionnaire/data path, including app-owned Name keyboard evidence, Age slider/no-IME evidence, panel-exit keyboard hide, explicit Enter-submit replay, export mirror equality, prior-experience prompt/export, final end-confirmation export, redness conversion/export, and simulated blink/runtime flash evidence.
- Keep hidden bridges, UISet `SpatialTextField` demographics ownership, Android `AndroidView(EditText)` demographics owners, Quest/system IME Name ownership, Age numeric `EditText`, Age trigger dial, custom loose keyboard ownership, and integrated/in-questionnaire keyboard layouts out unless the study design intentionally changes again. Future placement changes must be interpreted from the headset origin/radial visual angle, not flat screen coordinates.

## Previous Snapshot - 2026-06-13 Demographics App-Owned Keyboard Verified On APK

- Latest debug APK after replacing the fragile Quest/system IME Name path with the app-owned in-panel keyboard: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `C706C57E4B814AD920B74C187ECE834933581F8A886DF1D8D89B343D1B6372A2`, size `165366005` bytes.
- The previous raw IME validation reproduced the user's failure: `artifacts/quest-demographics-raw-ime/rawime_20260613_145439` showed that direct text sent through the old served-view/IME path left Name blank. The current fix removes the Quest/system IME dependency for Name.
- Name now uses a visible app-owned in-panel QWERTY keyboard that writes directly to `demographics.name`, supports Space/Back/Clear/Next, accepts direct hardware/ADB keyevents while focused, caps at 80 characters, and only moves to Age on explicit Next/Enter. Age remains an in-panel `ComposeSlider` from 0 to 100 writing `demographics.age`, with no IME owner.
- Current local gates passed on this APK/source state: build via `tools/build-apk.ps1`, keyboard contract `artifacts/native-keyboard-validation/native-keyboard-validation-20260613-152759.json`, static study validation `artifacts/local-validation/validation-20260613-153139.json`, and app-owned keyboard layout preview `artifacts/layout-previews/preview-20260613-152759/demographics-app-owned-keyboard-preview.png`.
- Current Quest 3S `3487C10J0P01ZY` headset gates passed on this APK: direct raw keyevent validation `artifacts/quest-demographics-direct-keyboard/20260613-152127/quest-demographics-direct-keyboard-validation-summary.json` with screenshots `demographics-direct-keyboard-name-entry.png` and `demographics-direct-keyboard-after-entry.png`; focused demographics smoke `artifacts/quest-demographics-keyboard/20260613-152350/quest-demographics-keyboard-validation-summary.json`; repeated-entry stress `artifacts/quest-demographics-keypress-stress/20260613-152428/quest-demographics-keypress-stress-summary.json`; qkv regression `artifacts/qkv/20260613-152518/quest-keyevent-questionnaire-validation-summary.json`; and panel/glitch smoke `artifacts/quest-panel-smoke/20260613-152617/quest-panel-smoke-summary.json`.
- The direct raw keyevent gate installed/uploaded the APK to the headset, focused Name, sent `Georgex`, Backspace, Space, `Fejer`, Enter, then set Age to `34` with D-pad events. It verified `BRB_DEMOGRAPHICS_TEXT_VALUE field=name ... value=george_fejer length=12 source=hardware_key_event`, `BRB_DEMOGRAPHICS_NAME_BACKSPACE ... source=hardware_key_event`, `BRB_DEMOGRAPHICS_TEXT_EDITOR_ACTION ... source=hardware_key_event`, Age `34`, and no `BRB_SOFT_KEYBOARD_REQUEST reason=field_name`.
- Keep hidden bridges, UISet `SpatialTextField` demographics ownership, Android `AndroidView(EditText)` demographics owners, Quest/system IME Name ownership, Age numeric `EditText`, Age trigger dial, and loose/custom keyboard paths out unless the study design intentionally changes again.

## Current Snapshot - 2026-06-13 New-Agent Integration Brief

- The public `New-Agent-Integration-Brief.md` is now implemented as a native contract, not a Unity or standalone-panel runtime dependency. Startup logs `BRB_AGENT_INTEGRATION_CONTRACT`, and JSON exports include top-level `agentIntegrationProtocol`.
- `agentIntegrationProtocol` records the source repo/branch, `unityDependency=false`, `rustyXrBrokerRequired=false`, in-process questionnaire routing, direct native Polar PMD ECG/RR as active brokerless physiology, disabled diagnostic-only Unity-compatible LSL defaults, stable `HeartbeatPulseDriver` blink routing, and `controller_contact` as the final participant proof.
- The questionnaire section also stores the external `quest.questionnaire.v1` bridge shape to use only if the standalone panel is adopted later: explicit intent, `request_json`, caller-owned `content://` result URI, write grant, one-shot immutable broadcast `PendingIntent`, caller-side readback, and no ADB/public-storage/file/overlay/package-kill product route.
- Validation now requires the contract in `tools/validate-study.ps1`, `tools/validate-export-schema.ps1 -Synthetic`, and `tools/run-quest-keyevent-questionnaire-validation.ps1`. The mapping is documented in `docs/new-agent-integration-audit.md`.
- Latest debug APK after this integration-contract refresh: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `F272AAECC705ED533198D5C35FD401B68141A8D72714DF82E9E09629EE2560FC`, size `165363133` bytes. Full local preflight passed at `artifacts/local-preflight/20260613-024935/local-preflight-summary.json`.

## Current Snapshot - 2026-06-13 Demographics Focused Name Fix + Age Slider

- Latest debug APK after the demographics Name/Age fix: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `C8BE8451BDEF57C3CF2CAA0E3C7761CABF888A1A943B124973B85D8AADDAA0CA`, size `164492109` bytes.
- Demographics changed intentionally because manual Quest IME entry still accepted only one character in Name/Age. Name remains the only native keyboard field, using the visible `AndroidView(EditText)` path with text/capitalized-words, `IME_ACTION_NEXT`, max 80 characters, and no partial-name auto-advance. The activity no longer consumes Name text key events; focused EditText owns committed text, and Compose no longer writes stale state, `inputType`, `imeOptions`, or filters back into the field while it is focused.
- Age is no longer a Quest/system keyboard target. It is an in-panel `ComposeSlider` from 0 to 100, writes the same `demographics.age` export field as a stringified integer, logs `BRB_DEMOGRAPHICS_AGE_SLIDER_CONTRACT`, `BRB_DEMOGRAPHICS_AGE_SLIDER_VALUE`, and `BRB_DEMOGRAPHICS_AGE_SLIDER_DONE`, and is validated as no-IME with no `BRB_SOFT_KEYBOARD_REQUEST reason=field_age`.
- The intended validation contract is now Name text keyboard evidence plus Age slider/no-IME evidence. Keep hidden bridges, UISet `SpatialTextField` demographics ownership, Age numeric `EditText`, the old Age trigger dial, and loose/custom keyboard paths out unless the study design changes again.
- Current passing gates on that APK: native keyboard contract `artifacts/native-keyboard-validation/native-keyboard-validation-20260613-134342.json`, standalone/static validation `artifacts/local-validation/validation-20260613-134343.json`, Quest panel/glitch visual smoke `artifacts/quest-panel-smoke/20260613-134123/quest-panel-smoke-summary.json`, focused Quest demographics smoke `artifacts/quest-demographics-keyboard/20260613-133249/quest-demographics-keyboard-validation-summary.json`, heavier Quest demographics stress `artifacts/quest-demographics-keypress-stress/20260613-133339/quest-demographics-keypress-stress-summary.json` with 12 repeated visible-EditText Name updates plus D-pad Age slider to `34`, and full qkv regression `artifacts/qkv/20260613-133433/quest-keyevent-questionnaire-validation-summary.json` proving Name keyboard, Age slider/no-IME, panel-exit hide, export mirror equality, and questionnaire/export routing. The latest layout preview for the unchanged Name/Age visual layout remains `artifacts/layout-previews/preview-20260613-032227/demographics-native-keyboard-preview.png`.

## Current Snapshot - 2026-06-13 02:36

- Latest debug APK after the heartbeat stable-surface fix: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `5E65AFFB84D93CFC71A95F6188A84DED46917CB7D2286172CCF8C34B10BB4A1D`.
- ECG/mock blinking now keeps the original `BigRedButton.glb` as the sole visible button model. Runtime markers report `BRB_BUTTON_GLOW_STABLE_SURFACE_READY`, `modelGlow=stable_idle_model_native_lights`, `actualGlowPath=stable_idle_model_native_lights`, `geometrySwap=false`, `shapeStable=true`, `placementStable=true`, and `scaleStable=true`; generated GLB glow variants are retained only as retired provenance/reference assets.
- Quest 3S no-press visual evidence is in `artifacts/quest-glow-stable-surface/20260613-024056`: `button-glow-off.png`, `button-glow-on.png`, and `button-glow-bounds-comparison.json`. The saturated-red cap bounds are stable within a 1 px screenshot-threshold tolerance between off/on in both eyes: widths stay unchanged, one eye is exact, and the other differs by only `deltaHeightPx=1` with `deltaCenterYPx=-0.5`.
- `tools/build-apk.ps1` passed for this APK. `tools/validate-study.ps1 -SkipBuild` wrote `artifacts/local-validation/validation-20260613-022638.json`; the new heartbeat stable-surface checks pass. That validator state is superseded by the demographics Age-slider contract update above, which changes the old Age `AndroidView(EditText)` expectation intentionally.

## Current Snapshot - 2026-06-12 15:28

- Latest debug APK after returning demographics to the reliable visible Android `EditText` pattern: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `3077C6276054967BA1946E11B53898002CC062B98510F9DBBB6B02F023100CE3`, size `114899300` bytes.
- Demographics now uses visible `AndroidView(EditText)` fields for both Name and Age, embedded in the BRB warm-paper field shell. Name is text/capitalized words with `IME_ACTION_NEXT` and max 80 chars. Age is numeric with `IME_ACTION_DONE`, digits-only sanitizer, and max 3 chars. Runtime markers report `platformControl=EditText`, `focusedView=EditText`, `inputOwner=androidViewEditText`, and `restartInput=true`.
- Local gates passed for this source state: `artifacts/native-keyboard-validation/native-keyboard-validation-20260612-152429.json`, `artifacts/local-validation/validation-20260612-152457.json`, and `tools/build-apk.ps1`.
- Focused demographics keyboard validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/quest-demographics-keyboard/20260612-152527/quest-demographics-keyboard-validation-summary.json`. It proves visible EditText Name and Age contracts, Name text/Next, Age number/Done, retarget logging, mixed Age cleanup `a1234 -> 123`, final `34`, and the current APK hash.
- Fast Quest directional questionnaire/data validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/qkv/20260612-152636/quest-keyevent-questionnaire-validation-summary.json`. It preserves questionnaire/export flow, `ExperimentResults` mirror pull, export mirror byte match, Name text-keyboard evidence, Age numeric-keyboard evidence, panel-exit keyboard hide, final prompt gating, redness conversion/export, and feedback/sham blink proof on the current APK hash.
- If manual headset typing still fails, the next explicit fallback should be a visible app-owned single keyboard with rectangular letters plus a digit pad writing directly to highlighted Name/Age state. Do not reintroduce hidden bridges, UISet `SpatialTextField` demographics ownership, or the Age trigger dial by accident.
- Remaining external gates are unchanged: a short human headset smoke for physically typing Name and Age, live Polar H10 PMD ECG smoke, fast human controller-contact smoke, and full physical controller-contact/live-H10 export validation.

## Current Snapshot - 2026-06-12 15:10

- Latest debug APK after replacing the brittle Age keyboard field with a controller-trigger numeric dial: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `F7A5D80CBA3927193D1D347BC1BA576A854CCB9112BE4A642E9B3E4F8F37BAF0`, size `114899300` bytes.
- Demographics now uses the native Quest/system keyboard for Name only. Age is intentionally not an IME target; it uses a same-row trigger dial with `-10`, `-`, value, `+`, and `+10` targets and writes the same `demographics.age` string through the existing digits-only/max-three sanitizer. No custom keyboard, hidden `EditText`, raw `AndroidView(EditText)`, or Age `SpatialTextField` path should return.
- Local gates passed for this source state: `artifacts/native-keyboard-validation/native-keyboard-validation-20260612-150734.json`, `artifacts/local-validation/validation-20260612-151157.json`, `artifacts/layout-previews/preview-20260612-151138`, and `tools/build-apk.ps1`.
- Focused demographics keyboard/age-selector validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/quest-demographics-keyboard/20260612-150821/quest-demographics-keyboard-validation-summary.json`. It proves actual Name `SpatialTextField` focus and text keyboard request, Age dial focus, no `BRB_SOFT_KEYBOARD_REQUEST reason=field_age`, mixed Age cleanup `a1234 -> 123`, dial confirm/increment to `34`, and final Done.
- Fast Quest directional questionnaire/data validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/qkv/20260612-150938/quest-keyevent-questionnaire-validation-summary.json`. It preserves the questionnaire/export flow, `ExperimentResults` mirror pull, export mirror byte match, Name keyboard evidence, Age trigger-dial evidence, panel-exit keyboard hide, final prompt gating, redness conversion/export, and feedback/sham blink proof on the current APK hash.
- Remaining external gates are unchanged: a short human headset smoke for physically typing Name and operating the Age dial with the controller trigger, live Polar H10 PMD ECG smoke, fast human controller-contact smoke, and full physical controller-contact/live-H10 export validation.

## Current Snapshot - 2026-06-12 14:35

- Latest debug APK after the reliable demographics keyboard fix: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `E323F9AE9084F213BB9233A805EAC2123D31E42BFF9ECBA38DE22ADCBF24D3B7`.
- Name and Age still use native Quest/Meta UISet `SpatialTextField` owners only. There is no custom keyboard, hidden `EditText`, bridge layer, export/schema change, or participant-flow change.
- Demographics focus now has one shared path for taps, Name Next, and stable-name auto-advance. Name and Age are equal-width, taller fields with full-cell invisible activation hitboxes; Age retarget clears previous focus, briefly hides the old keyboard, then requests numeric Age focus with bounded show/focus retries.
- Local gates passed for this source state: `artifacts/native-keyboard-validation/native-keyboard-validation-20260612-143352.json`, `artifacts/local-validation/validation-20260612-143909.json`, and `tools/build-apk.ps1`.
- Focused demographics keyboard validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/quest-demographics-keyboard/20260612-143451/quest-demographics-keyboard-validation-summary.json`. It now requires actual Name/Age `BRB_DEMOGRAPHICS_SPATIAL_TEXT_FIELD_FOCUS` markers plus text/numeric `BRB_SOFT_KEYBOARD_REQUEST` markers, not only focus-request markers.
- Remaining external gates are unchanged: a short human headset smoke for physical system-keyboard interaction, live Polar H10 PMD ECG smoke, fast human controller-contact smoke, and full physical controller-contact/live-H10 export validation.

## Current Snapshot - 2026-06-11 01:53

- Latest debug APK after guided demographics keyboard stabilization and qkv final-prompt replay gating: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `2E6654D11593A5CF4059BB7A0E90612D8FAE21AE3684EE63B5E984BA2F169732`.
- Demographics now uses a guided single-owner UISet flow: Name and Age remain one Meta UISet `SpatialTextField` owner each, with BRB warm-paper styling, required-label highlighting, automatic stable Name-to-Age retarget, age-specific numeric focus/show retries, age mixed-input cleanup, and digit/backspace fallback through the shared sanitizer. The raw `AndroidView(EditText)` and hidden bridge paths remain forbidden.
- Local gates passed for this source state: `artifacts/native-keyboard-validation/native-keyboard-validation-20260611-015021.json`, `artifacts/local-validation/validation-20260611-015023.json`, and `tools/build-apk.ps1`.
- Focused demographics keyboard validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/quest-demographics-keyboard/20260611-015122/quest-demographics-keyboard-validation-summary.json`. It proves the UISet keyboard contract, Name `George`, Name-to-Age retarget, mixed Age cleanup `a1234 -> 123`, clear, digit-by-digit Age `3` then `4`, and final `34` Done on the current APK hash.
- Fast Quest directional questionnaire/data validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/qkv/20260611-015215/quest-keyevent-questionnaire-validation-summary.json`. It preserves the full questionnaire/export flow, export mirror byte match, native keyboard lifecycle evidence, final prompt `OPTIONS_READY` before replay, and header-readable no-H10 ECG time-series CSV handling on the current APK hash.
- Remaining external gates are unchanged: live Polar H10 PMD ECG smoke, fast human controller-contact smoke, and full physical controller-contact/live-H10 export validation.

## Current Snapshot - 2026-06-11 00:41

- Demographics keyboard stabilization now uses one Meta UISet `SpatialTextField` input owner per Name/Age field, visually overlaid inside the existing Big Red Button Institute warm-paper field shell. The raw `AndroidView(EditText)` and hidden bridge routes are intentionally forbidden after live MR testing showed they did not reliably become the served IME target.
- Name uses text/capitalized-words input with max 80 characters. Age uses numeric input and sanitizes to digits/max three characters. Runtime markers now report `platformControl=SpatialTextField`, `inputOwner=uisetSpatialTextField`, field focus requests/retargets, and shared validation-intent value application for exact automated proof.
- Local validation passed for the current keyboard contract: `artifacts/native-keyboard-validation/native-keyboard-validation-20260611-003928.json` and `artifacts/local-validation/validation-20260611-003929.json`.
- Current debug APK was rebuilt at `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `E430AC14892C3ABEA38926463A17816F369929218C3FEADBD559D212EF9F7601`.
- Focused demographics keyboard validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/quest-demographics-keyboard/20260611-003943/quest-demographics-keyboard-validation-summary.json`. Because ADB cannot reliably focus/type into the Quest overlay keyboard, this gate proves contract, focus-request/retarget markers, and exact `George`/`34` values through the same sanitizer/value logging route used by the visible fields; a human headset smoke should still manually focus and type through the system keyboard before participant use.
- Fast Quest directional questionnaire/data validation also passed on Quest 3S `3487C10J0P01ZY`: `artifacts/qkv/20260611-004051/quest-keyevent-questionnaire-validation-summary.json`. It kept the full questionnaire/export flow passing for the rebuilt APK while preserving the final physical 3D button interaction contract.
- The pre-condition prior Big Red Button experience prompt now has supplied voice clips for the question, Yes feedback, No feedback, and the pre-start instructions clip. Participant mode hides the Yes/No options until the question audio completes, locks the first selected answer, hides the unchosen option, plays only the selected feedback clip, then plays `pre_start_instructions.mp3` before revealing `Start experiment`. Hidden fast validation shortcuts the audio gate with explicit markers. The start action logs each click and reports Polar H10 ECG readiness blocks visibly instead of failing silently.

## Current Snapshot - 2026-06-10 22:25

- Fresh test pass on the current pushed commit `b14ded0` / APK SHA-256 `8EF7C02833AFFDDE200F68086C9A638963B2B02129F4BD5305C14AB95837E9AB`, size `107318779` bytes.
- Full local preflight passed: `artifacts/local-preflight/20260610-222213/local-preflight-summary.json`. This rebuilt/checked the APK, passed static validation, exact instruction-audio validation, synthetic export schema validation, native keyboard contract, physical-evidence validator tests, final hardware post-run audit tests, final hardware post-run binding validation, and layout preview rendering.
- Short Quest smoke suite passed on Quest 3S `3487C10J0P01ZY`: `artifacts/quest-smoke-suite/20260610-222240/quest-smoke-suite-summary.json`. It ran visual-layout smoke and panel/glitch smoke, installed/launched the current APK, verified the modeled button layout path, verified questionnaire panel/glitch startup, and pulled fresh screenshots.
- Fast Quest directional questionnaire/data validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/qkv/20260610-222348/quest-keyevent-questionnaire-validation-summary.json`. It verified generated demographics, one-time prior button prompt, emulated condition button counts, native keyboard lifecycle, redness VAS/Likert conversion and transcript `microTimeline` cue logs, ECG/audio-window fields, pulled `ExperimentResults`, and byte-matched the export mirror.
- Current readiness report: `artifacts/readiness-report/report-20260610-222522/readiness-report.md`, status `ready_except_physical_and_live_polar_gates`. Current goal audit: `artifacts/goal-completion-audit/20260610-222531/goal-completion-audit.md`, status `ready_except_physical_and_live_polar_gates`, `completionAllowed=false`.
- Remaining gates are still external/human: live Polar H10 PMD ECG smoke, fast human controller-contact smoke, and full physical controller-contact/live-H10 export validation.

## Current Snapshot - 2026-06-10 22:16

- The two questionnaire-change clips now drive transcript-synced micro-event choreography instead of one broad swap animation. `RednessConversionMicroEvent` rows encode phrase windows, spoken cue, participant caption, visual cue, and intensity. Condition 1 includes events such as `supervisor_ping`, `swap_requested`, `seven_boxes_assemble`, `answer_already_given`, and `result_settle`; condition 2 includes `professional_warning`, `mid_experiment_freeze`, `restore_requested`, `boxes_erased`, `data_importance`, and `wrong_way_settle`.
- Runtime logging now includes `BRB_REDNESS_SCALE_CONVERSION_MICRO_EVENT` at participant-facing micro-event starts, and `BRB_REDNESS_SCALE_CONVERSION_CUE` logs a `microTimeline` even in qkv `validationShortcut=true` mode. Routine qkv remains fast but now proves the shortcut log contains the first and second clip micro-event timelines.
- `RednessConversionChoreographyOverlay` now changes its visual behavior per active transcript event: supervisor memo ticks, row targeting brackets, swap tears, seven-box assembly, preserved-answer marker, warning strikes, frozen boxes, box erasure, track reconstruction, data-ledger flicker, and final wobble/settle. The save action remains blocked until the full supplied changeover clip finishes.
- Layout previews now render transcript micro-event rails in `redness-changeover-vas-to-likert-preview.png` and `redness-changeover-likert-to-vas-preview.png`; static validation fails if the key micro-event codes, runtime marker, qkv microTimeline checks, or preview rails disappear.
- Current debug APK after this update: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `8EF7C02833AFFDDE200F68086C9A638963B2B02129F4BD5305C14AB95837E9AB`, size `107318779` bytes.
- Full local preflight passed: `artifacts/local-preflight/20260610-221547/local-preflight-summary.json`. It includes build, static validation, exact instruction-audio validation, synthetic export schema validation, native keyboard contract, physical-evidence validator tests, final hardware post-run audit validator tests, final hardware post-run binding validation, and layout preview rendering.
- Fast Quest directional questionnaire/data validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/qkv/20260610-221158/quest-keyevent-questionnaire-validation-summary.json`, APK SHA-256 `8EF7C02833AFFDDE200F68086C9A638963B2B02129F4BD5305C14AB95837E9AB`. It validated the stricter redness cue log requirement with `microTimeline` event names, preserved the native keyboard lifecycle checks, exported both redness formats/orders, pulled and byte-compared `BigRedButtonFirstStudyExports` and `ExperimentResults`, and preserved ECG/audio-window/export checks.
- Current readiness report: `artifacts/readiness-report/report-20260610-221611/readiness-report.md`, status `incomplete` only because live Polar H10 PMD ECG smoke, fast human controller-contact smoke, and full physical controller-contact/live-H10 export validation remain external gates. Current goal audit: `artifacts/goal-completion-audit/20260610-221621/goal-completion-audit.md`, status `ready_except_physical_and_live_polar_gates`, `completionAllowed=false`.
- Current final hardware wrapper dry run for this APK: `artifacts/final-hardware-gates/20260610-221422/final-hardware-gates-summary.json`, `dryRun=true`, APK match `true`, post-run audit `pass`.

## Current Snapshot - 2026-06-10 21:45

- The two questionnaire-change MP3s were recovered from Gmail raw MIME, staged under `../audio-assets/questionnaire`, and packaged as Android raw resources `first_questionnaire_change.mp3` and `second_questionnaire_change_excuse.mp3`. SHA-256 hashes are `F49538CB8B41959C95B760F6A7CF5B63D2BDC9D979C40739A10DA2AAED50570D` and `E008B68E8C158A4CD86D89E695A38AB8FC71573F1507051EEBDCFB1C7C796CDC`.
- Whisper transcripts were written to `artifacts/questionnaire-change-transcripts/`. The first clip is about 22.988 s and now choreographs VAS-to-Likert conversion: unstable VAS during the supervisor setup, scale swap at 7.2 s when Likert is requested, answer-carry/settle near 19.3 s, then editing reopens. The second clip is about 16.771 s and choreographs Likert-to-VAS conversion: unstable Likert during the design-swap explanation, scale swap at 7.3 s when visual analogue scale is requested, settle near 14.64 s, then editing reopens.
- Runtime markers now use `BRB_REDNESS_SCALE_CONVERSION_CUE ... placeholder=false`, `BRB_REDNESS_SCALE_CONVERSION_CHOREOGRAPHY state=start|swap|settle|end`, and `BRB_PICTOGRAPHIC_SAVE_BLOCKED reason=redness_conversion_choreography` if a participant tries to save before the clip finishes. Fast qkv replay still converts immediately and logs `validationShortcut=true` so routine directional/export validation does not wait through the two changeover clips.
- Current debug APK after adding the questionnaire-change choreography: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `1EA96C0169A6F900DEBC566A3848BA28E74EDB93F8210AA54F334D1B236076D2`, size `107318779` bytes.
- Full local preflight passed: `artifacts/local-preflight/20260610-214008/local-preflight-summary.json`. It rebuilt the APK, passed static study validation `artifacts/local-validation/validation-20260610-214010.json`, exact instruction-audio validation, synthetic export schema validation, native keyboard contract, physical-evidence validator tests, final hardware post-run audit tests, final hardware post-run binding validation, and layout previews. The latest layout previews include `redness-changeover-vas-to-likert-preview.png` and `redness-changeover-likert-to-vas-preview.png`.
- Fast Quest directional questionnaire/data validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/qkv/20260610-214803/quest-keyevent-questionnaire-validation-summary.json`, APK SHA-256 `1EA96C0169A6F900DEBC566A3848BA28E74EDB93F8210AA54F334D1B236076D2`. It confirmed the real cue log markers via validation shortcut mode (`first_questionnaire_change`, `second_questionnaire_change_excuse`, `placeholder=false`, `validationShortcut=true`), exported both redness formats/orders, pulled and validated both `BigRedButtonFirstStudyExports` and `ExperimentResults`, and preserved ECG/audio-window/export checks.
- `tools/run-quest-keyevent-questionnaire-validation.ps1` now has the same foreground-relaunch guard as visual smoke after a stale `com.example.rustyxr.opengles` foreground task caused a no-`BRB_` timeout. The successful qkv run followed that script fix.
- Current readiness report: `artifacts/readiness-report/report-20260610-215210/readiness-report.md`, status `incomplete` by design because live Polar H10 and human controller-contact/full physical export gates are still missing. It does show the current APK/qkv/local evidence and a current final hardware wrapper dry run: `artifacts/final-hardware-gates/20260610-215210/final-hardware-gates-summary.json`, `dryRun=true`, APK match `true`, post-run audit `pass`.

## Current Snapshot - 2026-06-10 18:50

- Latest debug APK after adopting more of MesmerPrism's Unity heartbeat-glow behavior: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `55009BA38CC5FFB99614FC058B51B7D9F11291740527E73DDFEE21FBF41A64E5`, size `107318603` bytes.
- Heartbeat/RR glow now uses 32 generated GLB material variants rather than 7. The variants are created by `tools/create-button-glow-variants.py`; peak cap material remains `baseColorFactor=1.0,0.085,0.025,1.0` and `emissiveFactor=3.35,0.095,0.026`, with subtle metal-bezel warmth and the dark base material preserved.
- Native heartbeat pulse timing now matches the Unity blink controller more closely: `HEARTBEAT_PULSE_DURATION_MS = 320L`, `pulseCurve=unity_ease_in_out_1_to_0`, `HEARTBEAT_PULSE_REFRACTORY_MS = 250L`, and 16 visual update frames at 20 ms. This replaces the earlier exponential/short-frame decay.
- Fresh Quest 3S screenshots are in `artifacts/quest-glow-screenshots/20260610-184928`: `button-glow-off.png` and `button-glow-on.png`. Runtime logs report `BRB_BUTTON_GLOW_MODEL_VARIANTS_READY ... variants=32 ... surfaceGeometry=false transparentHalo=false` and `BRB_VISUAL_GLOW_VALIDATION ... actualGlowPath=glb_material_variant_swap`.
- Static validation passed at `artifacts/local-validation/validation-20260610-184813.json`, including checks for the 32 GLB assets, opaque material-emission generator, Unity pulse timing markers, and rejection of transparent/additive halo paths.
- Remaining external gates are unchanged: live Polar H10 PMD ECG streaming and human controller-contact/full physical export validation still require a headset/H10 operator run before the overall project goal can be marked complete.

## Current Snapshot - 2026-06-10 17:51

- Latest debug APK after increasing heartbeat glow contrast while preserving the idle/base `BigRedButton.glb`: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `F4D0057EF783D7CF89BB344098C989CEBEC30270D7AE6F86FBCB2060524BD93B`, size `107316303` bytes.
- The glow contrast was increased by regenerating `app/src/main/assets/models/glow/BigRedButtonGlowLevel01.glb` through `07.glb` with `tools/create-button-glow-variants.py`. The current peak cap material is `baseColorFactor=1.0,0.085,0.025,1.0` and `emissiveFactor=3.35,0.095,0.026`; native cap-surface point lights now peak at `3.20` with warmer RGB `1.0,0.05,0.02`.
- Fresh Quest 3S screenshots are in `artifacts/quest-glow-screenshots/20260610-175113`: `button-glow-off.png` shows the preserved idle red surface, and `button-glow-on.png` shows a stronger red/orange material-emission state. The capture was gated on `BRB_VISUAL_GLOW_VALIDATION ... actualGlowPath=glb_material_variant_swap`; logcat reports `surfaceGeometry=false transparentHalo=false`.
- Static validation passed at `artifacts/local-validation/validation-20260610-175028.json`, including the new `button glow variant generator uses opaque material emission` check.
- The canonical idle `BigRedButton.glb` and pictographic thumbnail hashes were intentionally left unchanged in this pass to avoid collateral changes outside the heartbeat flash. If future visual review still wants more contrast, the next escalation is to deliberately darken/desaturate the idle model material and update the model/thumb hashes together.
- Remaining external gates are unchanged: live Polar H10 PMD ECG streaming and human controller-contact/full physical export validation still require a headset/H10 operator run before the overall project goal can be marked complete.

## Current Snapshot - 2026-06-10 17:02

- Latest debug APK after replacing the retired halo/canopy glow attempts with the MesmerPrism-style GLB material-variant surface glow: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `45084AEB6F736012D1D0AA75DC1FBFC4FE26A346AD9E108FCB113B29DC33544D`, size `107316303` bytes.
- Heartbeat/RR blinking is now visually validated on Quest 3S `3487C10J0P01ZY` with the actual app implementation, not a local preview. Fresh headset screenshots are in `artifacts/quest-glow-screenshots/20260610-170249`: `button-glow-off.png` shows the idle GLB surface, and `button-glow-on.png` shows the cap itself warming into a brighter red/orange emissive material state. The capture was gated on `BRB_VISUAL_GLOW_VALIDATION ... actualGlowPath=glb_material_variant_swap`.
- Static validation passed at `artifacts/local-validation/validation-20260610-170206.json`. It checks that the participant-facing glow path is `modelGlow=glb_material_variant_swap`, that all seven `models/glow/BigRedButtonGlowLevelXX.glb` variants exist, that `BRB_BUTTON_GLOW_MODEL_VARIANTS_READY` is emitted, and that transparent halo/canopy paths, translucent/additive cap shells, and the disabled 2D radial panel fallback are not the primary glow path.
- Meta Spatial SDK currently exposes material controls for app-created materials but not a Unity-like mutable material handle on loaded GLB `Mesh` renderers. The active native approximation is therefore idle/original GLB plus seven precomputed emissive cap-material GLB variants assisted by small native point lights. Do not reintroduce participant-facing transparent geometry to fake glow.
- Remaining external gates are unchanged: live Polar H10 PMD ECG streaming and human controller-contact/full physical export validation still require a headset/H10 operator run before the overall project goal can be marked complete.

## Current Snapshot - 2026-06-10 15:32

- Latest debug APK after implementing the MesmerPrism Unity transplant plan in the native Quest app: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `19359ED6791D683B0DCD9D6CCAE086776F58B5C1CB659BA65C583BB2DF5C98AF`, size `107315659` bytes.
- Button press contact now uses a seven-box cap approximation (`shape=multi_box_cap`) instead of a single broad target: one centered cap box plus six ring boxes. `ButtonContactLatch` latches by input source so held/sliding contact cannot repeatedly count as new presses. Runtime logs include target names, accepted controller/hand contact markers, and `BRB_BUTTON_CONTACT_LATCH`.
- Heartbeat/RR blinking now goes through `HeartbeatPulseDriver` and GLB material variants (`BRB_BUTTON_GLOW_MODEL_VARIANTS_READY`, `modelGlow=glb_material_variant_swap`). The app swaps from the idle `BigRedButton.glb` to seven progressively brighter/emissive cap-material GLBs under `models/glow/`, tonemapped to a hot red/orange native peak with small native point lights only as a material-light assist. ECG/RR feedback is glow-only; geometric press motion is reserved for accepted button presses. Broad cap-shell/canopy geometry, translucent dome overlays, and the old 2D radial panel glow are not participant-facing paths; the 2D fallback remains disabled via `MODEL_GLOW_PANEL_FALLBACK_ENABLED = false`.
- ECG exports now include pulse metadata on `*_ecg_blink_events.csv`, native threshold R-peak detector diagnostics in `*_ecg_detector_events.csv`, and a disabled-by-default external/LSL signal scaffold in `*_external_signal_samples.csv`. LSL remains only a schema/contract marker (`BRB_LSL status=disabled`) until an Android-compatible `liblsl.so`/JNI path is explicitly added and headset-tested.
- Full local preflight passed: `artifacts/local-preflight/20260610-153759/local-preflight-summary.json`. It rebuilt the APK, passed static validation `artifacts/local-validation/validation-20260610-153801.json`, exact audio validation `artifacts/audio-validation/audio-validation-20260610-153801.json`, synthetic export schema validation `artifacts/export-schema-validation/export-schema-validation-20260610-153801.json`, native keyboard contract, physical-evidence validator tests, final hardware post-run audit tests, final hardware post-run audit binding validation, and layout preview rendering.
- Remaining external gates are unchanged: live Polar H10 PMD ECG streaming and human controller-contact/full physical export validation still require a headset/H10 operator run before the overall project goal can be marked complete.

## Current Snapshot - 2026-06-10 15:20

- Latest debug APK after adding whole-panel contour disruption and a stronger central corrupted buffer wheel to questionnaire intro/outro glitches: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `AE448388B4F9F0754FE35521B775CA359ED5178EFD87EB5100A8D50832B0132F`, size `107315659` bytes.
- The questionnaire panel shell now wobbles during glitch transitions, with interrupted/dropped contour segments, dynamic edge-originating tear masks, duplicated cyan/magenta outline offsets, reduced paper opacity during failure phases, and apparent surface slices so the panel no longer reads as a perfectly stable rectangle during the cue.
- The corrupted loading/buffer wheel now draws as the final overlay layer with a high-contrast dark backing disk, broken cyan/white/magenta arcs, dropped spokes, buffer ticks, and a jittered hub so it remains visible in still headset captures.
- Latest static validation passed: `artifacts/local-validation/validation-20260610-152000.json`; latest build passed with the APK hash above.
- Latest Quest panel/glitch smoke passed on Quest 3S `3487C10J0P01ZY`: `artifacts/quest-panel-smoke/20260610-152025/quest-panel-smoke-summary.json`. Fresh screenshots: `demographics-panel.png` and `pictographic-panel.png` in that folder show the interrupted panel silhouette and central buffer wheel. This smoke mode did not start condition audio or create exports.
- Existing supplied transition MP3s remain unchanged and hash-locked. No instruction MP3, export schema, native keyboard behavior, panel placement contract, or button interaction contract changed for this update.

## Current Snapshot - 2026-06-10 14:49

- Latest debug APK after intensifying questionnaire intro/outro glitch aesthetics: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `7A9A7123CEBE067F9A0A6C4CC1DAA1F0BB00D13EC3ADA0E8F6BB99492E764925`, size `107315659` bytes.
- Questionnaire transition visuals are now a comfort-safe phased blue system-failure effect. Intro uses acquisition/rupture/reassembly/lock timing; outro uses destabilize/dropout/collapse/dead-screen timing. The renderer adds panel jitter, dense scanline tears, macroblocks, cyan/white/blue color breakup with limited magenta contamination, border desynchronization, noise bursts, non-textual online/offline cues, and a corrupted buffer/loading spinner inspired by a spinning wait cursor.
- The existing supplied transition MP3s remain unchanged and hash-locked as `questionnaire_intro_glitch.mp3` and `questionnaire_outro_glitch.mp3`; no instruction MP3, export schema, native keyboard behavior, panel placement, or button interaction contract changed for this glitch update.
- Latest full local preflight passed: `artifacts/local-preflight/20260610-144945/local-preflight-summary.json`. It rebuilt/checked the current APK, passed static validation, exact audio validation, synthetic export schema validation, native keyboard contract, physical-evidence validator tests, final hardware post-run audit validator tests, final hardware post-run audit binding validation, and layout preview rendering.
- Latest standalone/static validation passed: `artifacts/local-validation/validation-20260610-144947.json`. It includes dedicated checks for the phased comfort-safe failure overlay, corrupted buffer spinner, and non-textual online/offline system-failure cues.
- Quest headset panel-smoke screenshots have not yet been refreshed for this intensified glitch. The older `20260610-132413` screenshots show the previous blue scanline treatment, not this current renderer.
- Remaining external gates are unchanged: standalone live Polar H10 PMD ECG streaming, fast human controller-contact smoke, and full physical controller-contact/live-H10 export validation still require a human operator wearing the Quest and Polar H10.

## Current Snapshot - 2026-06-10 14:35

- Latest debug APK after adding the one-time prior Big Red Button experience prompt: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `EA6600EA52D75EB1B4821CE934E87D5C0001CCC46E9E26EE33DB47218D254073`, size `107315659` bytes.
- New participant-flow insertion: after consent/demographics exits, before condition 1 starts, the 3D Big Red Button model/collider stay hidden and the transparent button/counter panel asks: `Oh wait, we have just one more question: Do you have any experience with pressing big red buttons?` The participant chooses Yes/No checkboxes. `No` shows `No? Well than you are in for a treat!`; `Yes` shows `An experienced user, just the type of participant we need.` A `Start experiment` button then starts condition 1 and reveals the modeled button. This prompt is one-time only and does not repeat before condition 2.
- New logged variable: JSON now includes `priorBigRedButtonExperience` with `question`, `answer`, `hasExperience`, `timestampIso`, `shownBeforeCondition=1`, and `displayLocation=button_counter_panel`. Summary CSV now includes `prior_big_red_button_experience`, `prior_big_red_button_experience_bool`, and `prior_big_red_button_experience_timestamp_iso`.
- Latest full local preflight passed: `artifacts/local-preflight/20260610-143526/local-preflight-summary.json`. It rebuilt/checked the current APK, passed static validation, exact audio validation, synthetic export schema validation, native keyboard contract, physical-evidence validator tests, final hardware post-run audit validator tests, final hardware post-run audit binding validation, and layout preview rendering.
- Latest standalone/static validation passed: `artifacts/local-validation/validation-20260610-143528.json`. It includes dedicated checks that the prior-experience XR prompt lives in the transparent counter panel, disables the button model/collider until `Start experiment`, logs the new variable, and remains passable through direction plus enter replay.
- Latest synthetic export schema validation passed: `artifacts/export-schema-validation/export-schema-validation-20260610-143529.json`; it requires the new `priorBigRedButtonExperience` JSON object and summary CSV columns.
- Latest local layout previews: `artifacts/layout-previews/preview-20260610-143538`, including `pre-button-experience-prompt-preview.png`.
- Latest fast Quest directional questionnaire/data validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/qkv/20260610-143217/quest-keyevent-questionnaire-validation-summary.json`. It installed APK SHA-256 `EA6600EA52D75EB1B4821CE934E87D5C0001CCC46E9E26EE33DB47218D254073`, selected the prior-experience prompt via app-side controller replay (`right`, then `enter`), confirmed the prompt marker appears once before condition 1 and not before condition 2, exported `answer=yes` / `hasExperience=true` to JSON and summary CSV, pulled both `BigRedButtonFirstStudyExports` and `ExperimentResults`, and kept the existing keyboard, redness, ECG-window, simulated blink, and mirror-equality proofs.
- Latest final hardware wrapper dry run for the current APK: `artifacts/final-hardware-gates/20260610-143424/final-hardware-gates-summary.json`, status `dry_run`, APK SHA-256 `EA6600EA52D75EB1B4821CE934E87D5C0001CCC46E9E26EE33DB47218D254073`. It was run with `-SkipPreRunHandoff` because the pre-run handoff currently treats incomplete live-H10/controller-contact evidence as a hard error; post-run readiness and goal-audit binding still passed.
- Latest final hardware post-run audit binding verifier: `artifacts/final-hardware-postrun-audit-validation/validation-20260610-143538/final-hardware-postrun-audit-validation.json`, status `pass`, binding the current dry-run wrapper to its generated readiness and goal-audit files.
- Latest readiness/goal audit generated by the dry run remain incomplete by design until external gates pass: readiness `artifacts/readiness-report/report-20260610-143425/readiness-report.json`; goal audit `artifacts/goal-completion-audit/20260610-143426/goal-completion-audit.json`, `completionAllowed=false`.
- Remaining external gates are unchanged: standalone live Polar H10 PMD ECG streaming, fast human controller-contact smoke, and full physical controller-contact/live-H10 export validation still require a human operator wearing the Quest and Polar H10.

## Current Snapshot - 2026-06-10 13:48

- Latest debug APK after removing the retired custom keyboard implementation and tightening final-wrapper audit bootstrapping: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `55E3B082077BD55D939D1610E5FEC7AC56641B8FA62C201804AEE322CC951C2A`, size `107315659` bytes.
- Latest full local preflight passed: `artifacts/local-preflight/20260610-132615/local-preflight-summary.json`. It rebuilt/checked the current APK and passed static study validation, exact audio validation, synthetic export schema validation, `tools/test-native-keyboard-contract.ps1`, physical-evidence validator tests, `tools/test-final-hardware-postrun-audit-validator.ps1`, final hardware post-run audit binding validation, and layout preview rendering.
- Latest standalone/static validation passed: `artifacts/local-validation/validation-20260610-132618.json`. It checks compact no-scroll demographics, native system keyboard text/numeric focus hooks, IME retarget/restart markers, absence of the removed custom loose-keyboard implementation, calibrated pictographic controls, redness VAS/Likert conversion/export fields, warm heartbeat emission overlay, dual controller/hand contact provenance, final physical precheck backlinking, explicit readiness-source binding for goal audits, final hardware post-run audit validator coverage, final hardware post-run audit behavioral-test readiness reporting, final hardware post-run audit binding validation readiness reporting, explicit final-wrapper summary binding through `-FinalHardwareGateSummaryPath`, validated dry-run wrapper selection, visual-smoke full-log fallback evidence, the final-wrapper post-run audit contract, the goal-completion audit contract, the goal-audit `final_hardware_postrun_audit_chain` software requirement row, the final operator handoff contract, and the ECG/audio/export/physical-gate contracts.
- Latest native keyboard contract passed: `artifacts/native-keyboard-validation/native-keyboard-validation-20260610-132618.json`. It proves Name requests text mode, Age requests numeric mode, no custom loose-keyboard code/panel/markers remain in the activity, age strips non-digits/maxes at three digits, and the system IME retarget path is present.
- Latest final hardware post-run audit validator behavioral test passed: `artifacts/final-hardware-postrun-audit-tests/t-20260610-132625/final-hardware-postrun-audit-validator-test-summary.json`. It proves valid dry-run, valid real-pass, and valid incomplete-bootstrap wrapper/readiness/goal-audit chains are accepted, while stale readiness links, status mismatches, dry-run completion allowance, incomplete-readiness completion allowance, APK hash mismatches, and failed post-run audit status are rejected.
- Latest local layout previews: `artifacts/layout-previews/preview-20260610-132626`. It includes `demographics-native-keyboard-preview.png`, documenting the central questionnaire plus native movable Quest keyboard panel and automatic text-to-number retargeting.
- Latest short Quest smoke suite passed on Quest 3S `3487C10J0P01ZY` for the current APK: `artifacts/quest-smoke-suite/20260610-132347/quest-smoke-suite-summary.json`. It includes visual-layout evidence at `artifacts/quest-visual-layout-smoke/20260610-132347/button-condition-screenshot.png`, with `facingParticipant=true`, `downwardAngleDeg=26.6`, `angularDiameterDeg=34.3`, and panel/glitch evidence at `artifacts/quest-panel-smoke/20260610-132413/demographics-panel.png` plus `pictographic-panel.png`.
- Latest fast Quest directional questionnaire/data validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/qkv/20260610-132449/quest-keyevent-questionnaire-validation-summary.json`. It records APK SHA-256 `55E3B082077BD55D939D1610E5FEC7AC56641B8FA62C201804AEE322CC951C2A`, native keyboard text/numeric mode evidence, text-to-number retarget evidence, panel-exit keyboard hide evidence, Enter-submit replay evidence, redness conversion cue evidence, pulled readable JSON/CSV files from both export folders, exact ECG audio-window proof, simulated blink/runtime flash proof, and `exportMirrorMatched=true`.
- Latest fresh live Polar H10 PMD ECG smoke attempt on Quest 3S `3487C10J0P01ZY`: `artifacts/qpolar/20260610-134648/quest-polar-h10-live-smoke-summary.json`, status `fail`, APK SHA-256 `55E3B082077BD55D939D1610E5FEC7AC56641B8FA62C201804AEE322CC951C2A`, `detected=false`, `connected=false`, `pmdReady=false`, `ecgStreaming=false`, `ecgSamples=0`, `requestedMtu=70`, `expectedRequestedMtu=70`. It waited 45 seconds and proves the current-APK failure-summary path only, not live H10 proof.
- Latest fresh no-operator controller-contact smoke attempt on Quest 3S `3487C10J0P01ZY`: `artifacts/qcs/20260610-114301/quest-controller-contact-smoke-summary.json`, status `fail`, APK SHA-256 `55E3B082077BD55D939D1610E5FEC7AC56641B8FA62C201804AEE322CC951C2A`, `pressDetected=false`, `controllerContactPresses=0`. This is current-APK failure-summary evidence only, not physical press proof.
- Latest full physical/live-H10 export gate attempt on Quest 3S `3487C10J0P01ZY`: `artifacts/qpv/20260610-115825/quest-physical-press-validation-summary.json`, status `fail`, current APK SHA-256 `55E3B082077BD55D939D1610E5FEC7AC56641B8FA62C201804AEE322CC951C2A`, `polarPrecheckSkipped=false`, `condition1ControllerPresses=0`, `condition2ControllerPresses=0`, and `polarPrecheckSummary=artifacts/qpolar/20260610-115826/quest-polar-h10-live-smoke-summary.json`. The full run was correctly not started because no worn H10 was detected.
- Latest final hardware wrapper dry run: `artifacts/final-hardware-gates/20260610-130944/final-hardware-gates-summary.json`, status `dry_run`, current APK SHA-256 `55E3B082077BD55D939D1610E5FEC7AC56641B8FA62C201804AEE322CC951C2A`, `preRunHandoff.status=pass`, and `postRunAudit.status=pass`. The wrapper now writes a pre-run operator handoff, automatically writes a readiness report and goal completion audit after dry runs, failures, and real passes unless `-SkipPostRunAudit` is supplied, and passes its own summary path into readiness with `-FinalHardwareGateSummaryPath` so the post-run audit binds to the wrapper that just ran.
- Latest final hardware post-run audit binding verifier: `artifacts/final-hardware-postrun-audit-validation/validation-20260610-132626/final-hardware-postrun-audit-validation.json`, status `pass`. It proves `artifacts/final-hardware-gates/20260610-130944/final-hardware-gates-summary.json`, wrapper-generated readiness `artifacts/readiness-report/report-20260610-130948/readiness-report.json`, and wrapper-generated goal audit `artifacts/goal-completion-audit/20260610-130949/goal-completion-audit.json` agree on readiness linkage and APK hash, and that the dry run does not allow completion.
- Latest readiness report: `artifacts/readiness-report/report-20260610-134802/readiness-report.md`, status `ready_except_physical_and_live_polar_gates`, APK SHA-256 `55E3B082077BD55D939D1610E5FEC7AC56641B8FA62C201804AEE322CC951C2A`. It cites the current full local preflight, current Quest smoke suite, current qkv run, fresh current-APK H10/contact/full-physical failure summaries, current final hardware wrapper dry run `artifacts/final-hardware-gates/20260610-130944/final-hardware-gates-summary.json`, exact ECG audio-window proof, simulated blink/runtime flash proof, native keyboard lifecycle proof, redness conversion proof, export mirror equality, latest local validation `artifacts/local-validation/validation-20260610-132618.json`, `finalHardwarePostRunAuditValidatorTestPass=true`, and `finalHardwarePostRunAuditValidationPass=true`.
- Latest goal completion audit: `artifacts/goal-completion-audit/20260610-134803/goal-completion-audit.json`, status `ready_except_physical_and_live_polar_gates`, `readinessJson=artifacts/readiness-report/report-20260610-134802/readiness-report.json`, `softwareRequirementsProven=true`, `externalHardwareRequirementsProven=false`, `completionAllowed=false`. It includes a proven `final_hardware_postrun_audit_chain` software row linked to `artifacts/final-hardware-postrun-audit-tests/t-20260610-132625/final-hardware-postrun-audit-validator-test-summary.json` and `artifacts/final-hardware-postrun-audit-validation/validation-20260610-132626/final-hardware-postrun-audit-validation.json`. Missing requirement IDs are `live_polar_h10_streaming`, `human_controller_contact_smoke`, and `full_physical_live_h10_export`.
- Latest final operator handoff: `artifacts/final-operator-handoff/handoff-20260610-134804/final-operator-handoff.json`, status `ready_for_operator_external_gates`, `readyForOperatorExternalGates=true`, `completionAllowed=false`, `softwareAuditReadyForOperator=true`, and `adbDeviceReadyWhenRequested=true` for Quest 3S serial `3487C10J0P01ZY` using the project-local ADB path. It confirms the current APK hash, readiness report, goal audit, final post-run audit-chain row, and ADB readiness agree before a human/H10 operator starts `tools/run-final-hardware-gates.ps1`.
- The Button Experience task now treats the horizontal closeness control as a distance-from-self visual axis. The slider thumb and button marker move in the same direction; internally `feltCloseness0To100` still means higher is closer, and `selfButtonDistanceUnits` carries the visible distance axis. Endpoint labels are `very close`/`very distant`; presence endpoints are `small presence`/`large presence`.
- A third Button Experience response now asks `How red did the button feel?`. In condition 1 it starts as a VAS labelled `slightly red` to `very red`, then converts once to seven Likert boxes after VAS selection. In condition 2 it starts as seven Likert boxes, then converts once to a VAS after Likert selection. JSON and summary CSV store both final formats plus `rednessScaleOrder`; the apology/supervisor audio cue is currently represented by a dedicated placeholder marker/cue until the supplied clip is provided.
- The demographics panel is compact/no-scroll in the 1180x820 Quest panel: participant ID remains hidden, gender is four-choice, handedness is three-choice, and the signature field remains a trigger/pointer drawing pad. Name and age use only the native Quest/system keyboard, which is movable/closable by the participant. The app requests text mode for Name, numeric mode for Age, forces IME retarget/restart on focus changes, and age sanitizes typed input to digits.
- The heartbeat visual flash is now a material-emission-style GLB variant swap on the modeled button body, rather than a flat 2D halo or a transparent overlay.
- The button contact path now supports controller contact plus hand-tracked collider selects. Hand contact is logged/exported as `hand_contact` and summarized separately from `controller_contact`; it is supplemental usability support and does not replace the final controller-contact proof requirement unless the protocol is explicitly changed.
- Remaining external gates are unchanged: standalone live Polar H10 PMD ECG streaming and full physical controller-contact/live-H10 export validation still need a human operator wearing the headset and H10.

## Previous Snapshot - 2026-06-10 08:39

- Latest debug APK after minimum-MTU Polar update: `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `ADB18B597183C59160A3545B3658B8F707EAE99DDD9F83D32515472B0FAAEB5E`, size `107315659` bytes. Local preflight rebuilt/checked it on 2026-06-10 08:04.
- Latest local preflight passed: `artifacts/local-preflight/20260610-080444/local-preflight-summary.json`. It rebuilt/checked the APK and passed static study validation, exact audio validation, synthetic export schema validation, strengthened physical-evidence validator tests, and layout preview rendering.
- Latest raw-ECG/static-tooling validation passed after tightening live-Polar smoke checks, readiness evidence selection, condition/ECG anchoring, final real-Polar timing-shape checks, and the final hardware wrapper contract: `artifacts/local-validation/validation-20260610-083917.json`; it checks that readiness requires `artifacts/qkv` evidence on the current APK, qkv keyboard/Enter markers, simulated ECG blink/runtime flash markers, failed hardware summary paths, highest advertised Polar PMD ECG settings, minimum ECG MTU 70, live-smoke MTU-70 enforcement, exact qkv ECG windows in milliseconds and nanoseconds, a `BRB_CONDITION_AUDIO_START_ANCHOR anchor=pre_media_player_start` marker before `MediaPlayer.start()`, `elapsed_ns`/audio-window ECG CSV columns, byte-comparable `ExperimentResults` mirrors, citation of the newest standalone/static local validation artifact, final physical-evidence rejection of coarse or non-monotonic real-Polar ECG timing, and `tools/run-final-hardware-gates.ps1` sequencing.
- Latest exact audio validation passed: `artifacts/audio-validation/audio-validation-20260610-080450.json`; condition MP3 hashes/durations remain unchanged.
- Latest raw-ECG synthetic export schema validation passed: `artifacts/export-schema-validation/export-schema-validation-20260610-080450.json`; it requires `*_ecg_timeseries.csv` with `elapsed_ns`, audio-window start/end/duration columns, and exact per-condition capture-window metadata.
- Latest physical-evidence validator test passed: `artifacts/ppe-tests/t-20260610-080450/physical-evidence-validator-test-summary.json`; it rejects missing real ECG rows, missing real blink rows, bad real sample rate, short real sample coverage, invalid PMD package metadata, invalid requested MTU 70, missing low-latency config, non-monotonic `elapsed_ns`, and coarse/clumped real-Polar sample timing.
- Latest local layout previews: `artifacts/layout-previews/preview-20260610-080458`.
- Latest short Quest smoke suite passed on Quest 3S `3487C10J0P01ZY` for the current APK: `artifacts/quest-smoke-suite/20260610-080652/quest-smoke-suite-summary.json`. It includes visual-layout evidence at `artifacts/quest-visual-layout-smoke/20260610-080652` and panel/glitch evidence at `artifacts/quest-panel-smoke/20260610-080718`.
- Latest fast Quest directional questionnaire/data validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/qkv/20260610-080525/quest-keyevent-questionnaire-validation-summary.json`. It records APK SHA-256 `ADB18B597183C59160A3545B3658B8F707EAE99DDD9F83D32515472B0FAAEB5E`, keyboard request/hide evidence, Enter-submit replay evidence for each questionnaire stage, pulled readable JSON, summary CSV, press-event CSV, ECG blink-event CSV, and raw ECG time-series CSV from both `BigRedButtonFirstStudyExports` and `ExperimentResults`; the pulled `ExperimentResults` time-series CSV is `artifacts/qkv/20260610-080525/pulled/ExperimentResults/brb_first_study_keyevent_ecg_timeseries.csv`. `exportMirrorMatched=true`; `artifacts/qkv/20260610-080525/export-mirror-comparison.json` reports 6 matched files and 0 mismatches. That historical qkv run predates the feedback/physiology split; current qkv runs must prove feedback-source complement, sham blink rows/runtime flash, press `elapsed_ns` columns, and no simulated rows in the real ECG time-series export.
- Latest live Polar H10 PMD ECG smoke attempt failed on Quest 3S `3487C10J0P01ZY`: `artifacts/qpolar/20260610-080829/quest-polar-h10-live-smoke-summary.json`. It waited 20 seconds, but no Polar H10-compatible BLE advertisement/status was detected; `detected=false`, `connected=false`, `pmdReady=false`, `ecgStreaming=false`, `ecgSamples=0`, `requestedMtu=70`, `expectedRequestedMtu=70`. This proves the current failure-summary path only, not live H10 streaming.
- Latest no-operator controller-contact smoke failure-summary test: `artifacts/qcs/20260610-080934/quest-controller-contact-smoke-summary.json`; expected status `fail`, `pressDetected=false`, `controllerContactPresses=0`, APK SHA-256 `ADB18B597183C59160A3545B3658B8F707EAE99DDD9F83D32515472B0FAAEB5E`. This proves the current failure path only, not physical press success.
- Latest short full-physical failure-summary test: `artifacts/qpv/20260610-081025/quest-physical-press-validation-summary.json`; expected status `fail`, `polarPrecheckSkipped=true`, zero controller-contact presses, no export JSON/CSV, and summary fields for the `ExperimentResults` mirror. This proves the timeout failure path only, not physical press success.
- Latest final hardware wrapper dry run passed: `artifacts/final-hardware-gates/20260610-083916/final-hardware-gates-summary.json`. It verifies command construction and ordering for live Polar H10 PMD ECG smoke, fast controller-contact smoke, and full controller-contact/live-H10 export validation on the current APK. It is not hardware evidence because `dryRun=true`.
- Latest readiness report: `artifacts/readiness-report/report-20260610-083924/readiness-report.md`, status `ready_except_physical_and_live_polar_gates`, APK SHA-256 `ADB18B597183C59160A3545B3658B8F707EAE99DDD9F83D32515472B0FAAEB5E`. Its software/headset gates include `questKeyeventQuestionnaireValidationPass=true`, `questKeyeventQuestionnaireApkHashMatchesCurrent=true`, `questKeyeventExperimentResultsPulled=true`, `questKeyeventExportMirrorMatched=true`, `questKeyeventEcgAudioWindowMatched=true` with condition 1 `300774/300774` ms and `300774000000/300774000000` ns, condition 2 `325590/325590` ms and `325590000000/325590000000` ns, `130 Hz` for both conditions, `questKeyeventEcgBlinkMatched=true` with simulated blink rows/runtime flash/time-series proof, plus `questKeyeventKeyboardLifecycleMatched=true` and `questKeyeventEnterSubmitMatched=true`; it cites the newest standalone/static local validation artifact `artifacts/local-validation/validation-20260610-083917.json`, requires `finalHardwareGateWrapperDryRunPass=true` from `artifacts/final-hardware-gates/20260610-083916/final-hardware-gates-summary.json` for software readiness, and also surfaces `questControllerContactSmokePass=false`, `questControllerContactSmokeApkHashMatchesCurrent=true`, `questPhysicalPressValidationPass=false`, and `polarH10LiveSmokePass=false` from the latest human/hardware attempts. The readiness tool can emit final status `complete` only after standalone live Polar H10 PMD ECG smoke and full physical controller-contact/live-H10 export validation both pass on the current APK.
- The in-condition button now has an old digital alarm/calculator-style red counter above the 3D model, driven by accepted button press count.
- Accepted button presses now play the temporary swappable button press cue `../audio-assets/localized/shared/button_press/sfx_9020_button_press_placeholder_kenney_bong.ogg`. The final replacement sound still needs stimulus review/hash-locking and later motion-envelope matching.
- Questionnaire multiple-choice and navigation controls now play short UI feedback cues from `../audio-assets/localized/shared/questionnaire_ui/`.
- The consent signature is now a trigger/pointer drawing pad, not a text field. It stores `brb_signature_strokes_v1` temporal stroke JSON in the existing `signature` export field.
- The questionnaire intro/outro overlay was strengthened into a more aggressive blue software-failure/BSOD-style glitch with stripe, block, tear, and flash layers.
- Polar H10 support is implemented through the standard BLE Heart Rate Service plus Polar PMD ECG. The intake menu shows a PMD-aware validity status strip: green check requires HR/RR plus raw PMD ECG samples streaming at 130 Hz; HR/RR-only detection remains a waiting state. The app counterbalances `real_polar_h10` vs `simulated_neurokit2` across conditions by reading prior exports.
- Simulated ECG/RR data is packaged at `app/src/main/assets/ecg/neurokit2_simulated_rr_intervals_ms.csv`, SHA-256 `80D612CEC91C511471F19347C0B76A997FAF0E4AB785E2003B10179C819801C1`, generated from NeuroKit2.
- ECG protocol metadata is exported in JSON, per-condition `ecgSource`/`ecgBlinkCount` fields are exported in summary CSV, full blink events are exported to `*_ecg_blink_events.csv`, and raw ECG waveform samples are exported to `*_ecg_timeseries.csv` in both headset export folders. The time-series CSV now includes `elapsed_ns` plus audio-window start/end/duration columns, and JSON/summary exports include exact millisecond and nanosecond capture-window fields. The final physical validator now requires one `real_polar_h10` condition with at least 95 percent expected 130 Hz ECG sample coverage inside the instruction-audio window, exact real-Polar ECG window duration, requested MTU 70 evidence, sequential sample indices, strictly increasing `elapsed_ns`, a median sample interval matching 130 Hz within tolerance, no large sample gaps, and at least one RR blink event.
- Each audio condition now logs `BRB_CONDITION_AUDIO_START_ANCHOR ... anchor=pre_media_player_start` and sets `startedElapsedNs` immediately before `MediaPlayer.start()`, so PMD ECG sample clipping cannot miss early audio samples because of start-call ordering. The condition still ends from `MediaPlayer` completion and exports the exact MP3 duration as the ECG window.
- Polar H10 support now uses the raw Polar PMD ECG stream in addition to the standard Heart Rate Service. The client requests high BLE connection priority, starts with the minimum ECG MTU 70 for low-latency/small-packet readout, retries larger MTUs only after invalid-MTU responses, selects the highest advertised PMD ECG sample rate/resolution before falling back to H10 defaults, decodes 3-byte signed ECG microvolt samples at 130 Hz on H10, timestamps samples through PMD frame nanosecond timing, and clips stored samples to the exact active instruction-audio window.
- `BRB_BUTTON_SPATIAL_LAYOUT` is now logged both at scene setup and at each condition start. This is required because short smoke scripts clear logcat immediately before launch and need a condition-window marker.
- Remaining external gates: live Polar H10 PMD ECG streaming validation with `tools/run-quest-polar-h10-live-smoke.ps1`, then human-worn Quest controller-contact export validation with `tools/run-quest-physical-press-validation.ps1` while wearing the Polar H10. The final physical script now runs the live Polar smoke first by default, pulls both `BigRedButtonFirstStudyExports` and `ExperimentResults`, validates both folders for real controller-contact presses plus real-Polar ECG/blink/time-series exact-window and high-resolution monotonic timing evidence, and byte-compares the mirror files. The latest readiness report intentionally keeps both open even though local preflight, short Quest smoke, and fast directional questionnaire/data validation all pass.

## Implemented

- Native Meta Spatial SDK app scaffold.
- Package `org.bigredbutton.firststudy`.
- Passthrough enabled on scene start.
- Locomotion disabled.
- Consent/demographics panel.
- Condition 1 and 2 audio-driven flow.
- Button press logging during condition stages.
- Post-condition pictographic scale.
- 14-item adapted IPQ presence questionnaire.
- Lost Opportunity VAS.
- Participant ID is now assigned under the hood and no longer appears as an intake text field.
- Gender is now a four-option intake choice: `male`, `female`, `other`, `prefer_not_to_say`. Handedness is a three-option intake choice: `left`, `right`, `ambidextrous`.
- Questionnaire panels now spawn through a current-gaze-line placement contract at `x=0`, `y=1.52`, `z=1.55`, with `scene.setViewOrigin(0,0,0,0)` immediately before showing. The app logs `BRB_QUESTIONNAIRE_PANEL_LAYOUT placement=current-gaze-line`.
- Demographics text fields request the native Quest/system keyboard on focus with field-specific text/numeric modes, force IME retarget/restart when switching between Name and Age, and hide the keyboard on Done or questionnaire-panel exit; markers include `BRB_SYSTEM_KEYBOARD_FIELD_CONTRACT`, `BRB_SOFT_KEYBOARD_REQUEST`, `BRB_SOFT_KEYBOARD_SWITCH`, and `BRB_SOFT_KEYBOARD_HIDE`.
- Questionnaire panels now use blue software-failure/glitch intro/outro transitions driven by `../audio-assets/localized/shared/questionnaire_transition/raw_0400_questionnaire_intro_glitch.mp3` and `../audio-assets/localized/shared/questionnaire_transition/raw_0410_questionnaire_outro_glitch.mp3`. The assets are hash-locked in static validation: intro SHA-256 `3B78AE610233BBCA731BE13E50E33F5C50D697E0A7170522C38E87923347F087`, outro SHA-256 `B57A15DA2E1CC9D93A8D915696663E8BABE293FD53A0E8FDD8604D51A931292F`.
- Completed sessions now export to both `BigRedButtonFirstStudyExports` and a SideQuest-friendly `ExperimentResults` folder under `/sdcard/Android/data/org.bigredbutton.firststudy/files/`.
- Fast directional questionnaire validation is available through `tools/run-quest-keyevent-questionnaire-validation.ps1` and launch extra `brb.keyeventValidation`. It shortcuts the audio wait, replays bounded up/down/left/right/enter-equivalent commands through the app controller-direction and submit handlers, validates native keyboard text/numeric mode, text-to-number retarget, panel-exit hide markers, pulls both export folders, and writes expected-vs-observed comparisons.
- Participant-facing questionnaire panels now use task titles and response instructions. They no longer expose instrument names, quotient names, adapted-IPQ rationale, or logged-variable text; those remain in documentation/export metadata for researchers. The Button Experience task directly asks how close and how present the Big Red Button felt by the end of the experience.
- The Button Experience task now uses a transparent thumbnail rendered directly from the same `BigRedButton.glb` geometry/materials at `app/src/main/res/drawable-nodpi/big_red_button_model_thumbnail.png`, SHA-256 `F551A4F469984301FB87B4E59B1094AD27FA3B0F14930A353C17AFC881A69B90`. The thumbnail is drawn with its center aligned to the presence-circle center.
- JSON, summary CSV, press-event CSV, and `session-index.jsonl` export code.
- Build, validate, install/launch, and export-pull scripts.
- Gradle stages the centralized runtime audio library from `../audio-assets/localized`; app-local duplicate MP3/WAV/OGG runtime audio files were removed.
- Packaged native 3D Big Red Button model added at `app/src/main/assets/models/BigRedButton.glb`, SHA-256 `4BA2C479EAE6A103ADCE0B7D0AB70C94A5F21A12435DD90ACD0071F66EF5F52B`.
- The participant-facing button visual is now the smooth realistic GLB model loaded through `com.meta.spatial.toolkit.Mesh` from `apk:///models/BigRedButton.glb`. The old low-poly sample asset was replaced because it looked too abstract in native passthrough.
- The model entity now preserves the GLB/material render path and no longer forces `defaultShaderOverride = SceneMaterial.UNLIT_SHADER`, which had flattened the button into a less realistic red blob.
- Procedural native 3D button visual with `SceneMesh.dome` plus base meshes remains disabled as a fallback only.
- The visible 2D Compose button was removed from the audio condition. The current Compose panel is a transparent hit target over the model.
- The native button now ports the original repo's study-facing press semantics: 0.48 m distance, 350 ms startup suppression, and 180 ms cooldown.
- The native button now has an explicit human-facing seated layout contract: 0.48 m forward reach distance, cap/contact center at `y=1.04`, nominal seated eye line at `y=1.28`, approximately 26.6 degrees downward viewing angle and 34.3 degrees apparent diameter. The app logs this as `BRB_BUTTON_SPATIAL_LAYOUT`, and `tools/validate-study.ps1` recomputes the angles from source constants.
- The documented final input requirement is now controller-based physical pressing of the 3D model, matching the original Unity reference interaction. Any transparent helper target must be controller-contact driven, aligned to the model, and treated as temporary until proven on headset.
- A model-aligned invisible ISDK box collider has been added over the button cap. It accepts `PointerEventType.Select` only when `IsdkSystem.getInteractionEventSourceBehavior(event)` reports `COLLIDER_HOVER_CONTACT_ACTUATE`, and logs accepted presses as `inputSource=controller_contact`.
- Press-event JSON/CSV now includes input provenance: `inputSource`/`input_source` and `validationAutomation`/`validation_automation`. Summary CSV also includes source-specific press counts per condition.
- A temporary CC0 button sound placeholder has been staged at `../audio-assets/localized/shared/button_press/sfx_9020_button_press_placeholder_kenney_bong.ogg` and is now played for each accepted button press. It remains a placeholder until the final sound stimulus is selected and motion-matched.
- The GLB `pressed` animation trigger now uses `PlaybackType.CLAMP` with explicit animation name `pressed`; the debug APK builds with this change.
- Audio validation now checks exact source MP3 SHA-256 hashes plus ffprobe durations, so accidental re-encoding or duration changes fail locally.
- `tools/validate-export-schema.ps1` validates pulled headset exports and has a synthetic local contract mode.
- Hidden Quest autorun validation mode is available through launch extra `brb.autoValidation`; `tools/run-quest-autovalidation.ps1` installs/launches it, waits for real MP3 completion, pulls exports, and validates JSON/CSV. It is not a physical input proof. Treat this as a release/timing gate, not a routine check after every edit.
- Hidden Quest physical press validation mode is available through launch extra `brb.physicalPressValidation`; `tools/run-quest-physical-press-validation.ps1` first runs the no-audio live Polar smoke unless `-SkipPolarPrecheck` is supplied, then installs/launches the physical mode, waits for real MP3 completion, auto-submits only non-press questionnaire fields, pulls both export folders, and requires `controller_contact` presses in logcat, JSON, and press-events CSV. It now also requires a worn Polar H10 and final exported real-Polar PMD ECG coverage/blink evidence with sequential monotonic high-resolution timing in the primary export and the SideQuest-friendly `ExperimentResults` mirror. It requires a human operator wearing the headset.
- Fast human-operated controller-contact smoke is available through `tools/run-quest-controller-contact-smoke.ps1`. It launches the same physical validation mode, waits only for a real `controller_contact` press in logcat, captures evidence under `artifacts/qcs/<runId>`, and force-stops the app. It does not validate full audio completion or JSON/CSV export.
- Live Polar H10 smoke is available through `tools/run-quest-polar-h10-live-smoke.ps1`. It launches the intake screen and waits for live BLE PMD ECG status: detection, connection, PMD readiness, `ecgStreaming=true`, nonzero `ecgSamples`, and `ecgSampleRateHz=130`. It requires a worn, awake Polar H10 near the headset and does not validate full experiment export timing.
- Hidden Quest panel/glitch smoke is available through launch extra `brb.panelSmoke`; `tools/run-quest-panel-smoke.ps1` captures demographics and first Button Experience panels, requires both questionnaire intro MP3/glitch markers, and fails if a condition starts or data exports are created.
- Local Gradle build passes after generated-asset staging and model-backed button changes.
- Local Gradle build passes after adding the ISDK contact collider and press provenance export fields.
- Old Quest model-smoke evidence `artifacts/quest-model-smoke/20260609-161335/model-condition-unlit.png` is now superseded for visual-quality claims because it used the old low-poly/unlit render path.
- Earlier full local preflight passed at `artifacts/local-preflight/20260610-015146/local-preflight-summary.json`. It built that debug APK and ran static study validation, exact audio validation, synthetic export schema validation, physical-evidence validator tests, and layout preview rendering.
- Earlier debug APK build passed on 2026-06-10 at `app/build/outputs/apk/debug/app-debug.apk`; SHA-256 `88D7C6EF85DE05B1610BAB6C2342606646084273C8455F86726998D87C84819D`, size `107315415` bytes.
- Earlier static validation passed at `artifacts/local-validation/validation-20260610-015149.json`; it includes checks for hidden participant ID, gender/handedness choice controls, Quest keyboard hooks, gaze-line panel placement, intro/outro glitch MP3s, blue glitch overlay, directional questionnaire handler, fast validation shortcut, and `ExperimentResults` export mirror.
- Earlier local layout preview evidence is `artifacts/layout-previews/preview-20260610-025047`; the demographics preview reflects background participant ID assignment, four-option gender, three-option handedness, the Polar H10 status strip, the larger pointer signature pad, and the digital press counter preview.
- Earlier fast Quest directional questionnaire/data validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/qkv/20260610-013737/quest-keyevent-questionnaire-validation-summary.json`. It confirmed generated participant ID prefix, demographic text/choice exports, two emulated button presses per condition, all pictographic/slider/IPQ expected numeric fields, press timestamps/provenance, keyboard request marker, intro/outro glitch markers, and pulled readable JSON/CSV from both `BigRedButtonFirstStudyExports` and `/sdcard/Android/data/org.bigredbutton.firststudy/files/ExperimentResults`.
- Earlier Quest panel/glitch smoke passed on Quest 3S `3487C10J0P01ZY`: `artifacts/quest-panel-smoke/20260610-014613/quest-panel-smoke-summary.json`. It captured demographics and first Button Experience panels and confirmed questionnaire intro MP3 plus blue glitch markers without starting condition audio or exporting data.
- Prior static validation on 2026-06-09 passed after adding direct GLB structure checks, realistic material checks, `pressed` animation checks, dimensions fitting the collider envelope, the hidden panel/chime smoke contract, the computed human-facing spatial layout check, the short Quest smoke suite script, `BRB_CONDITION_PRESS_SOURCES`, live physical-gate per-condition press counts, stricter final physical-gate checks for accepted contact-select/source-summary evidence, the local preflight script, the Quest smoke suite script, and the readiness-report script: `artifacts/local-validation/validation-20260609-194733.json`.
- Earlier exact audio hash/duration validation passed at `artifacts/audio-validation/audio-validation-20260610-015149.json`; condition 1 remains `300.773878` s and condition 2 remains `325.590204` s.
- Earlier synthetic export schema validation passed at `artifacts/export-schema-validation/export-schema-validation-20260610-015149.json`.
- Prior local layout preview evidence is `artifacts/layout-previews/preview-20260609-195325`; it includes the MR button layout, website-style demographics panel with `Start experiment`, direct closeness/presence Button Experience task with the smooth model-derived button thumbnail centered in the circle, two actual-item session-experience rating pages, and additional-time slider.
- Current requirement-by-requirement completion audit is `docs/completion-audit.md`. It records which local/headset gates are proven and keeps the goal open pending human physical controller-contact export validation.
- Final physical validation operator guide is `docs/physical-validation-operator-guide.md`. `tools/run-quest-physical-press-validation.ps1` now writes `operator-checklist.txt` into each `artifacts/qpv/<runId>` folder, calls `tools/validate-export-schema.ps1` and `tools/validate-physical-press-evidence.ps1` on both pulled export folders, and fails if logcat, JSON, or CSV contain `auto_validation` button presses, controller-contact button rows marked as validation automation, missing accepted contact-select markers, missing condition source summaries, source-summary/export count mismatches, missing real-Polar ECG/RR rows in either condition, simulated ECG rows in the real physiology export, insufficient ECG coverage, invalid PMD metadata, missing press-to-ECG linkage, non-monotonic/coarse ECG timing, or missing low-latency config markers.
- Local physical-evidence validator test passed at `artifacts/ppe-tests/t-20260610-062334/physical-evidence-validator-test-summary.json`. It confirms the reusable validator accepts clean controller-contact plus real-Polar evidence and rejects automation-contaminated evidence, missing accepted-select evidence, missing source-summary evidence, source-summary/export mismatches, missing real ECG rows, missing real blink rows, bad real sample rate, short real ECG coverage, invalid PMD package metadata, invalid requested MTU, and missing low-latency config.
- Prior debug APK build passed on 2026-06-09 at `app/build/outputs/apk/debug/app-debug.apk`; SHA-256 `F0636D5436AF62EB87C3533C3E9C4370C2900E729790BEAC068F18247982F99C`, size `107315247` bytes.
- Earlier short Quest smoke suite passed on Quest 3S `3487C10J0P01ZY`: `artifacts/quest-smoke-suite/20260610-014550/quest-smoke-suite-summary.json`. It installed that APK, ran visual-layout smoke, then ran panel/glitch smoke with `-SkipInstall`.
- Earlier readiness report passed at `artifacts/readiness-report/report-20260610-015205/readiness-report.json` and `artifacts/readiness-report/report-20260610-015205/readiness-report.md` with status `ready_except_physical_gate`. It points at APK hash `88D7C6EF85DE05B1610BAB6C2342606646084273C8455F86726998D87C84819D`, local preflight `artifacts/local-preflight/20260610-015146/local-preflight-summary.json`, and Quest smoke suite `artifacts/quest-smoke-suite/20260610-014550/quest-smoke-suite-summary.json`.
- Earlier short Quest visual-layout smoke passed on Quest 3S `3487C10J0P01ZY`: `artifacts/quest-visual-layout-smoke/20260610-014551/quest-visual-layout-smoke-summary.json` confirms install, hidden physical-validation launch, condition 1 start, GLB loading from `apk:///models/BigRedButton.glb`, `facingParticipant=true`, `downwardAngleDeg=26.6`, `angularDiameterDeg=34.3`, and no fatal marker. Screenshot `artifacts/quest-visual-layout-smoke/20260610-014551/button-condition-screenshot.png` shows the 3D button centered in front of the participant with the top cap visible in passthrough. This was force-stopped before full audio completion.
- Earlier Quest panel/glitch smoke passed on Quest 3S `3487C10J0P01ZY`: `artifacts/quest-panel-smoke/20260610-014613/quest-panel-smoke-summary.json` confirms demographics intro cue, first questionnaire intro cue, both blue glitch markers, first Button Experience panel readiness, no condition start, and no export creation. Screenshots `demographics-panel.png` and `pictographic-panel.png` show the website-style demographics panel and Button Experience panel in passthrough.
- Latest manual visual inspection notes are in `docs/visual-validation-notes.md`. They record the inspected local preview set `artifacts/layout-previews/preview-20260610-080458`, and the latest Quest screenshots are `artifacts/quest-visual-layout-smoke/20260610-080652` plus `artifacts/quest-panel-smoke/20260610-080718`.
- Prior one-off Quest panel/chime smoke `artifacts/quest-panel-smoke/20260609-183028/quest-panel-smoke-summary.json` and prior one-off Quest visual smoke `artifacts/quest-visual-smoke/20260609-181916/quest-visual-smoke-summary.json` are superseded by the latest smoke suite above.
- Latest quick Quest install/launch passed on Quest 3S `3487C10J0P01ZY`: `artifacts/quest-launch/20260609-175020/quest-launch-summary.json` confirms app launch, `BRB_STUDY_READY`, `BRB_BUTTON_MODEL_ASSET`, contact collider/input registration, GLB loading, and no fatal marker. This was a short launch gate only; no full audio condition was started.
- Short Quest contact-collider smoke validation passed on Quest 3S `3487C10J0P01ZY`: `artifacts/quest-contact-smoke/20260609-165439/logcat-filtered.txt` confirms `BRB_BUTTON_CONTACT_COLLIDER_READY`, `BRB_BUTTON_CONTACT_INPUT isdk=true active=true observer=registered`, GLB loading, and condition 1 start with no fatal runtime marker. This proves initialization only, not physical controller pressing.
- Full real-duration Quest autorun completed once on Quest 3S `3487C10J0P01ZY` on 2026-06-09. Summary evidence is in `artifacts/qav/20260609-170003/quest-autovalidation-summary.json`; original log evidence is in `artifacts/quest-autovalidation/20260609-170003/logcat-filtered.txt`; pulled exports are in the shortened path `artifacts/qav/20260609-170003/BigRedButtonFirstStudyExports`; export schema validation passed at `artifacts/export-schema-validation/export-schema-validation-20260609-171445.json`. The run used real MediaPlayer completion: condition 1 duration `300774` ms / elapsed `300890` ms with 3 `auto_validation` presses; condition 2 duration `325590` ms / elapsed `325726` ms with 4 `auto_validation` presses. Per the current project rule, do not repeat this slow 10.5-minute audio autorun as routine validation unless audio files, MediaPlayer timing, condition transitions, or export plumbing changed.
- `tools/run-quest-autovalidation.ps1`, `tools/run-quest-physical-press-validation.ps1`, and `tools/pull-exports.ps1` now pull device export files through a short `%TEMP%` path and store future run evidence under shorter artifact paths to avoid Windows/ADB path-length failures.

## Current Implementation Caveat

The visible stimulus is model-backed, and a native ISDK contact collider now exists, but final controller-based physical interaction still needs a headset operator gate. The current app-side validation presses prove timing/export plumbing, not physical contact with the model. Live Polar PMD streaming and full-condition real-Polar export coverage also remain external gates. The transparent Compose panel remains an interim fallback and must not be mistaken for the final participant path.

## Next Work

- With a headset operator and worn Polar H10 ready, prefer `tools/run-final-hardware-gates.ps1` to run the final gates in order: live H10 PMD ECG smoke, fast controller-contact smoke, then the full controller-contact/live-H10 export validation. The wrapper writes `artifacts/final-hardware-gates/<runId>/final-hardware-gates-summary.json`.
- Use the individual scripts only when debugging a single gate: `tools/run-quest-polar-h10-live-smoke.ps1` for live H10 PMD ECG, `tools/run-quest-controller-contact-smoke.ps1` for fast physical contact, and `tools/run-quest-physical-press-validation.ps1` for the slow full audio/export run. Use `-SkipPolarPrecheck` only for an intentional same-session rerun after a standalone live H10 smoke has already passed.
- Later, replace the placeholder press sound if desired, then retime button-cap motion to the final sound envelope and hash-lock the final sound.
- Do not rerun the no-human full real-duration autorun unless audio files, MediaPlayer condition timing, condition flow, or export plumbing changes. Routine checks should use `validate-audio-assets.ps1`, build/static/schema validation, and short Quest smoke.
